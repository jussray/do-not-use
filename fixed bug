 import React, { useState, useEffect, useRef } from 'react'; 
 import {
  Text,
  StyleSheet,
  TouchableOpacity,
 ScrollView,
  TextInput,
  View,
  Animated,
} from 'react-native';

import AsyncStorage from '@react-native-async-storage/async-storage';
export default function App() {

  const [growthPath, setGrowthPath] = useState('girl');
  const [circlePostText, setCirclePostText] = useState('');
  const [circlePosts, setCirclePosts] = useState([]);
  const [isRecording, setIsRecording] = useState(false);
  const [screen, setScreen] = useState('home');
  const [journalText, setJournalText] = useState('');
  const [mood, setMood] = useState('Happy');
  const [theme, setTheme] = useState('night');
  const [entries, setEntries] = useState([]);
  const [moodHistory, setMoodHistory] = useState([]);
  const [voiceNotes, setVoiceNotes] = useState([]);
  const [messageIndex, setMessageIndex] = useState(0);
  const [userRole, setUserRole] = useState('teen');
  const [sharedMoments, setSharedMoments] = useState([]);
  const [supportAlerts, setSupportAlerts] = useState([]);
  const [parentMessages, setParentMessages] = useState([]);
  const breatheAnim = useRef(new Animated.Value(1)).current;
  const comfortMessages = [
    { emoji: '🌙', text: "You've survived every hard day so far. That matters." },
    { emoji: '☁️', text: 'Rest is productive too. You are allowed to pause.' },
    { emoji: '💙', text: "Someone is glad you’re still here tonight." },
    { emoji: '🌧️', text: 'Bad moments are real. So is your strength.' },
    { emoji: '✨', text: 'You don’t need to be perfect to be loved.' },
    { emoji: '🫶', text: 'Your feelings are allowed here.' },
    { emoji: '🕯️', text: 'Soft moment. Slow breath. Stay with me.' },
  ];

  useEffect(() => {
    loadData();
  }, []);

  useEffect(() => {
  saveData();
}, [journalText, mood, theme, entries, moodHistory, voiceNotes, circlePosts]);
useEffect(() => {
  Animated.loop(
    Animated.sequence([
      Animated.timing(breatheAnim, {
        toValue: 1.1,
        duration: 2200,
        useNativeDriver: true,
      }),
  
      Animated.timing(breatheAnim, {
        toValue: 1,
        duration: 2200,
        useNativeDriver: true,
      }),
    ])
  ).start();
}, []);
  const saveData = async () => {
    try {
      await AsyncStorage.setItem('journalText', journalText);
      await AsyncStorage.setItem('mood', mood);
      await AsyncStorage.setItem('theme', theme);
      await AsyncStorage.setItem('entries', JSON.stringify(entries));
      await AsyncStorage.setItem('moodHistory', JSON.stringify(moodHistory));
      await AsyncStorage.setItem('voiceNotes', JSON.stringify(voiceNotes));
      await AsyncStorage.setItem('circlePosts', JSON.stringify(circlePosts));
    } catch (error) {
      console.log('Save error:', error);
    }
  };

  const loadData = async () => {
    try {
      const savedJournal = await AsyncStorage.getItem('journalText');
      const savedMood = await AsyncStorage.getItem('mood');
      const savedTheme = await AsyncStorage.getItem('theme');
      const savedEntries = await AsyncStorage.getItem('entries');
      const savedMoodHistory = await AsyncStorage.getItem('moodHistory');
      const savedVoiceNotes = await AsyncStorage.getItem('voiceNotes');
      const savedCirclePosts = await AsyncStorage.getItem('circlePosts');

      if (savedJournal !== null) setJournalText(savedJournal);
      if (savedMood !== null) setMood(savedMood);
      if (savedTheme !== null) setTheme(savedTheme);
      if (savedEntries !== null) setEntries(JSON.parse(savedEntries));
      if (savedMoodHistory !== null) setMoodHistory(JSON.parse(savedMoodHistory));
      if (savedVoiceNotes !== null) setVoiceNotes(JSON.parse(savedVoiceNotes));
      if (savedCirclePosts !== null) setCirclePosts(JSON.parse(savedCirclePosts));
    } catch (error) {
      console.log('Load error:', error);
    }
  };

const getBackground = () => {
  if (theme === 'flower') return '#3B0A2E';
  if (theme === 'rain') return '#071A3D';
  if (theme === 'forest') return '#052E16';
  if (theme === 'lavender') return '#2E1065';
  return '#3B1930';
};

  const getHeroText = () => {
    if (mood === 'Happy') return "I’m glad\nyou’re smiling\ntonight 🌤️";
    if (mood === 'Sad') return "I’m here with\nyou tonight ☁️";
    if (mood === 'Angry') return "Let it out,\nyou’re safe here 🔥";
    if (mood === 'Tired') return "Rest your heart\ntonight 🌙";
    return "Welcome back 🌙";
  };

  const getMoodEngine = () => {
  if (mood === 'Happy') {
    return {
      emoji: '🌤️',
      title: 'Your glow is growing.',
      message: 'Use this energy to build confidence, not pressure.',
      room: 'Bright Growth Room',
      action: 'Try one bold little step today.',
    };
  }

  if (mood === 'Sad') {
    return {
      emoji: '☁️',
      title: 'Tonight we move softly.',
      message: 'Growth can be quiet. Rest still counts.',
      room: 'Soft Recovery Room',
      action: 'Do one gentle thing for yourself.',
    };
  }

  if (mood === 'Angry') {
    return {
      emoji: '🔥',
      title: 'Your fire needs somewhere safe to go.',
      message: 'Anger can become boundaries, movement, and truth.',
      room: 'Release Room',
      action: 'Move your body or name what felt unfair.',
    };
  }

  if (mood === 'Tired') {
    return {
      emoji: '🌙',
      title: 'Rest is part of becoming.',
      message: 'You do not have to earn softness.',
      room: 'Low Power Growth Room',
      action: 'Choose the smallest possible win.',
    };
  }

  return {
    emoji: '💜',
    title: 'You are becoming.',
    message: 'Bippin2 grows with your energy.',
    room: 'Growth Room',
    action: 'Start where you are.',
  };
};
  const selectMood = (newMood) => {
    setMood(newMood);

    const newMoodEntry = {
      id: Date.now(),
      mood: newMood,
      date: new Date().toLocaleDateString(),
      time: new Date().toLocaleTimeString(),
    };

    setMoodHistory([newMoodEntry, ...moodHistory]);
  };
   
  const getBippin2Path = () => {
  if (growthPath === 'girl') {
    return {
      label: 'Girl Growth Path',
      bodySupport: '🩸 Cycle Support',
      bodyText: 'Periods, cramps, body changes, energy shifts, hygiene, and confidence.',
      confidenceText: 'Body confidence, friendship pressure, boundaries, and self-worth.',
    };
  }

  return {
    label: 'Boy Growth Path',
    bodySupport: '⚡ Body Change Support',
    bodyText: 'Voice changes, body growth, hygiene, energy, movement, and confidence.',
    confidenceText: 'Pressure, anger, loneliness, confidence, and learning to express feelings safely.',
  };
};
  
  const saveEntry = () => {
  if (journalText.trim() === '') return;

  const newEntry = {
    id: Date.now(),
    text: journalText,
    mood,
    date: new Date().toLocaleDateString(),
    time: new Date().toLocaleTimeString(),
  };

  setEntries([newEntry, ...entries]);
  setJournalText('');
};

const saveVoiceNote = () => {
  const newVoiceNote = {
    id: Date.now(),
    title: 'Practice voice note',
    date: new Date().toLocaleDateString(),
    time: new Date().toLocaleTimeString(),
  };

  setVoiceNotes([newVoiceNote, ...voiceNotes]);
};

const saveCirclePost = () => {
  if (circlePostText.trim() === '') return;

  const newPost = {
  id: Date.now(),
  text: circlePostText,
  date: new Date().toLocaleDateString(),
  time: new Date().toLocaleTimeString(),

  emotionalImpact: 0,
  supportEnergy: 0,
  comfortLevel: 0,

  reactions: {
    glow: 0,
    heart: 0,
    moon: 0,
  },
};

  setCirclePosts([newPost, ...circlePosts]);
  setCirclePostText('');
};

const reactToPost = (postId, reactionType) => {
  setCirclePosts(
    circlePosts.map((post) =>
      post.id === postId
        ? {
            ...post,
            reactions: {
              ...post.reactions,
              [reactionType]: post.reactions[reactionType] + 1,
            },

  emotionalImpact: post.emotionalImpact + 1,

  supportEnergy:
  reactionType === 'heart'
    ? post.supportEnergy + 1
    : post.supportEnergy,

comfortLevel:
  reactionType === 'moon'
    ? post.comfortLevel + 1
    : post.comfortLevel,

          }
        : post
    )
  );

  const newReactionMoment = {
    id: Date.now(),
    mood: 'Connected',
    date: new Date().toLocaleDateString(),
    time: new Date().toLocaleTimeString(),
    note: `Someone connected with your Bip using ${reactionType} ✨`,
  };

  setMoodHistory([newReactionMoment, ...moodHistory]);
};

 const changeMessage = () => {
  setMessageIndex((prevIndex) => (prevIndex + 1) % comfortMessages.length);
};
 const switchRole = () => {
  if (userRole === 'teen') {
    setUserRole('parent');
  } else {
    setUserRole('teen');
  }
};
const BottomNav = () => (
  <View style={styles.bottomNav}>
    {[
      ['home', '🏠', 'Home'],
      ['pages', '💜', 'Pages'],
      ['calm', '🌙', 'Calm'],
      ['circle', '🌐', 'Circle'],
      ['more', '☰', 'More'],
    ].map((item) => (
      <TouchableOpacity
        key={item[0]}
        onPress={() => setScreen(item[0])}
        style={styles.navItem}
      >
        <Text style={styles.navIcon}>{item[1]}</Text>

        <Text
          style={[
            styles.navText,
            screen === item[0] && styles.activeNavText,
          ]}
        >
          {item[2]}
        </Text>
      </TouchableOpacity>
    ))}
  </View>
);

 if (screen === 'pages') {
  return (
    <ScrollView
      contentContainerStyle={[
        styles.container,
        { backgroundColor: getBackground() },
      ]}
    >

      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>💜 Se’kret Pages</Text>

      <Text style={styles.subtitle}>
        Your private emotional world.
      </Text>

      <View style={styles.bityHeroCard}>

        <Text style={styles.bityHeroEmoji}>💜</Text>

        <Text style={styles.bityHeroTitle}>
          Built for expression, not pressure.
        </Text>

        <Text style={styles.bityHeroText}>
          Write it. Voice it. Save moments.
          Feel safely without performing.
        </Text>

      </View>

      <Text style={styles.sectionTitle}>
        ✍️ Se’kret Space
      </Text>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('journal')}
      >
        <Text style={styles.buttonText}>
          ✍️ Write It Out
        </Text>
      </TouchableOpacity>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('voice')}
      >
        <Text style={styles.buttonText}>
          🎙️ Bippin BRB
        </Text>
      </TouchableOpacity>

      <View style={styles.entryCard}>

        <Text style={styles.entryText}>
          💭 Floating thoughts.
        </Text>

        <Text style={styles.entryText}>
          🎧 Voice moments.
        </Text>

        <Text style={styles.entryText}>
          🔐 Private emotional space.
        </Text>

      </View>

      <Text style={styles.sectionTitle}>
        🌈 Moodboards
      </Text>

      <View style={styles.statsRow}>

        <View style={styles.statCard}>
          <Text style={styles.statEmoji}>🌙</Text>

          <Text style={styles.statLabel}>
            Late Night
          </Text>
        </View>

        <View style={styles.statCard}>
          <Text style={styles.statEmoji}>☁️</Text>

          <Text style={styles.statLabel}>
            Soft Feelings
          </Text>
        </View>

      </View>

      <View style={styles.statsRow}>

        <View style={styles.statCard}>
          <Text style={styles.statEmoji}>🎵</Text>

          <Text style={styles.statLabel}>
            Soundtrack Feelings
          </Text>
        </View>

        <View style={styles.statCard}>
          <Text style={styles.statEmoji}>📸</Text>

          <Text style={styles.statLabel}>
            Memory Drops
          </Text>
        </View>

      </View>

      <Text style={styles.sectionTitle}>
        🌙 Safe Reminder
      </Text>

      <View style={styles.parentBridgeCard}>

        <Text style={styles.cardEmoji}>
          🌙
        </Text>

        <Text style={styles.cardText}>
          Your Se’kret space stays yours.
        </Text>

        <Text style={styles.bridgeText}>
          Parent Bridge never reads your pages,
          voice notes, or private thoughts.
        </Text>

      </View>

            <BottomNav />

    </ScrollView>
  );
}

  if (screen === 'journal') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>✍️ Write It Out</Text>

      <Text style={styles.subtitle}>
        A private page for whatever your heart needs to say.
      </Text>

      <TextInput
        style={styles.journalInput}
        placeholder="Write your Se’kret here..."
        placeholderTextColor="#94A3B8"
        multiline
        value={journalText}
        onChangeText={setJournalText}
      />

      <TouchableOpacity style={styles.button} onPress={saveEntry}>
        <Text style={styles.buttonText}>Save Page 💜</Text>
      </TouchableOpacity>

      <Text style={styles.sectionTitle}>Saved Pages</Text>

      {entries.map((entry) => (
        <View key={entry.id} style={styles.entryCard}>
          <Text style={styles.entryDate}>
            {entry.date} • {entry.time} • {entry.mood}
          </Text>

          <Text style={styles.entryText}>{entry.text}</Text>
        </View>
      ))}

      <TouchableOpacity style={styles.button} onPress={() => setScreen('pages')}>
        <Text style={styles.buttonText}>Back to Se’kret Pages 💜</Text>
      </TouchableOpacity>

      <BottomNav />
    </ScrollView>
  );
}

 if (screen === 'calm') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>

      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>🌙 Bip Calmly</Text>

      <Text style={styles.subtitle}>
        Stay here a sec.
      </Text>

      <Animated.View
        style={[
          styles.circle,
          { transform: [{ scale: breatheAnim }] },
        ]}
      >
        <Text style={styles.circleText}>Breathe</Text>
      </Animated.View>

      <View style={styles.messageCard}>

        <Text style={styles.cardEmoji}>
          {comfortMessages[messageIndex].emoji}
        </Text>

        <Text style={styles.cardText}>
          {comfortMessages[messageIndex].text}
        </Text>

        <TouchableOpacity
          style={styles.smallButton}
          onPress={changeMessage}
        >
          <Text style={styles.smallButtonText}>
            Another Calm Thought ✨
          </Text>
        </TouchableOpacity>

        <View style={styles.entryCard}>
          <Text style={styles.entryText}>
            🫧 Unclench your jaw.
          </Text>

          <Text style={styles.entryText}>
            🌙 Relax your shoulders.
          </Text>

          <Text style={styles.entryText}>
            💧 Drink some water.
          </Text>

          <Text style={styles.entryText}>
            📵 Step away from the noise for a minute.
          </Text>
        </View>

      </View>

      <Text style={styles.sectionTitle}>
        🌙 Quick Resets
      </Text>

<TouchableOpacity
  style={styles.button}
  onPress={() => setScreen('comfort')}
>
  <Text style={styles.buttonText}>
    🚨 Need Extra Support?
  </Text>
</TouchableOpacity>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('mindReset')}
      >
        <Text style={styles.buttonText}>
          🌙 Start 7-Min Mind Reset
        </Text>
      </TouchableOpacity>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('bodyReset')}
      >
        <Text style={styles.buttonText}>
          🫧 Start 7-Min Body Reset
        </Text>
      </TouchableOpacity>

      <View style={styles.entryCard}>

        <Text style={styles.entryText}>
          🌙 Mind Reset helps slow racing thoughts.
        </Text>

        <Text style={styles.entryText}>
          🫧 Body Reset helps release stress physically.
        </Text>

      </View>

      <BottomNav />

    </ScrollView>
  );
}
if (screen === 'mindReset') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>
      <Text style={styles.welcome}>🌙 7-Min Mind Reset</Text>
      <Text style={styles.subtitle}>Slow the thoughts. Come back to you.</Text>
      <Text style={styles.entryDate}>
  Minute 1 of 7 🌙
</Text>
      <View style={styles.messageCard}>
        <Text style={styles.cardEmoji}>🌙</Text>
        <Text style={styles.cardText}>Minute by minute. No pressure. Just reset.</Text>
      </View>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>1️⃣ Drop your shoulders.</Text>
        <Text style={styles.entryText}>2️⃣ Breathe slower than your thoughts.</Text>
        <Text style={styles.entryText}>3️⃣ Name 3 things you can see.</Text>
        <Text style={styles.entryText}>4️⃣ Sip water before replying to anyone.</Text>
        <Text style={styles.entryText}>5️⃣ Let one thought pass without chasing it.</Text>
        <Text style={styles.entryText}>6️⃣ Remind yourself: I do not have to solve everything tonight.</Text>
        <Text style={styles.entryText}>7️⃣ One more breath. I’m still here.</Text>
      </View>
      <View style={styles.messageCard}>
  <Text style={styles.cardEmoji}>✨</Text>

  <Text style={styles.cardText}>
    Reset complete when your body feels even 1% softer.
  </Text>
</View>
      <TouchableOpacity style={styles.button} onPress={() => setScreen('calm')}>
        <Text style={styles.buttonText}>Back to Bip Calmly 🌙</Text>
      </TouchableOpacity>

      <BottomNav />
    </ScrollView>
  );
}
if (screen === 'bodyReset') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>
      <Text style={styles.welcome}>🫧 7-Min Body Reset</Text>
      <Text style={styles.subtitle}>Move the stress out softly.</Text>

      <View style={styles.messageCard}>
        <Text style={styles.cardEmoji}>🫧</Text>
        <Text style={styles.cardText}>No workout pressure. Just loosen up.</Text>
      </View>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>1️⃣ Roll your neck slowly.</Text>
        <Text style={styles.entryText}>2️⃣ Shrug your shoulders up, then drop them.</Text>
        <Text style={styles.entryText}>3️⃣ Shake your hands out.</Text>
        <Text style={styles.entryText}>4️⃣ Stretch your arms over your head.</Text>
        <Text style={styles.entryText}>5️⃣ Walk around the room for one minute.</Text>
        <Text style={styles.entryText}>6️⃣ Do a tiny dance break. Yes, really 😭</Text>
        <Text style={styles.entryText}>7️⃣ One deep breath. Tell your body thank you.</Text>
      </View>

      <TouchableOpacity style={styles.button} onPress={() => setScreen('calm')}>
        <Text style={styles.buttonText}>Back to Bip Calmly 🌙</Text>
      </TouchableOpacity>

      <BottomNav />
    </ScrollView>
  );
}

if (screen === 'growth') {
  const moodEngine = getMoodEngine();
  const bippin2Path = getBippin2Path();

  const emotionalImpactTotal = circlePosts.reduce(
    (total, post) => total + (post.emotionalImpact || 0),
    0
  );

  const supportEnergyTotal = circlePosts.reduce(
    (total, post) => total + (post.supportEnergy || 0),
    0
  );

  const comfortLevelTotal = circlePosts.reduce(
    (total, post) => total + (post.comfortLevel || 0),
    0
  );

  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>🌱 Bippin2</Text>

      <Text style={styles.subtitle}>
        Your growth space changes from your Home mood and your growth path.
      </Text>

      <View style={styles.bityHeroCard}>
        <Text style={styles.bityHeroEmoji}>{moodEngine.emoji}</Text>
        <Text style={styles.bityHeroTitle}>{moodEngine.title}</Text>
        <Text style={styles.bityHeroText}>{moodEngine.message}</Text>
      </View>

      <Text style={styles.sectionTitle}>✨ Current Growth Room</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>🏠 {moodEngine.room}</Text>
        <Text style={styles.entryText}>💜 Mood signal: {mood}</Text>
        <Text style={styles.entryText}>🌱 Next step: {moodEngine.action}</Text>
        <Text style={styles.entryText}>🧭 Path: {bippin2Path.label}</Text>
      </View>

      <Text style={styles.sectionTitle}>🫧 Body + Puberty Support</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>{bippin2Path.bodySupport}</Text>
        <Text style={styles.entryText}>{bippin2Path.bodyText}</Text>
        <Text style={styles.entryDate}>
          Private growth support. Not shared with Parent Bridge.
        </Text>
      </View>

      <Text style={styles.sectionTitle}>⚡ Confidence Growth</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>{bippin2Path.confidenceText}</Text>
        <Text style={styles.entryText}>✨ Today’s reminder: you are becoming, not failing.</Text>
      </View>

      <Text style={styles.sectionTitle}>📈 Mood Trackboard</Text>

      <View style={styles.entryCard}>
        {moodHistory.length === 0 ? (
          <Text style={styles.entryText}>
            Choose moods on Home and Se’kret will show your patterns here.
          </Text>
        ) : (
          moodHistory.slice(0, 5).map((item) => (
            <Text key={item.id} style={styles.entryText}>
              {item.mood} • {item.date}
            </Text>
          ))
        )}
      </View>

      <Text style={styles.sectionTitle}>🌐 Private Circle Impact</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>💜 Emotional Impact: {emotionalImpactTotal}</Text>
        <Text style={styles.entryText}>🫂 Support Energy: {supportEnergyTotal}</Text>
        <Text style={styles.entryText}>🌙 Comfort Given: {comfortLevelTotal}</Text>
        <Text style={styles.entryDate}>
          These numbers are for you — not the whole world.
        </Text>
      </View>

      <Text style={styles.sectionTitle}>🌱 Becoming Areas</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>💬 Social + Boundaries Growth</Text>
        <Text style={styles.entryText}>✨ Future Self</Text>
        <Text style={styles.entryText}>🧠 Emotional Intelligence</Text>
        <Text style={styles.entryText}>🌙 Rest + Routine Growth</Text>
      </View>

      <Text style={styles.sectionTitle}>💜 Quiet Progress</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>✍️ Pages saved: {entries.length}</Text>
        <Text style={styles.entryText}>🎙️ Voice Bips saved: {voiceNotes.length}</Text>
        <Text style={styles.entryText}>📈 Mood check-ins: {moodHistory.length}</Text>
        <Text style={styles.entryText}>🌐 Circle posts: {circlePosts.length}</Text>
      </View>

      <BottomNav />
    </ScrollView>
  );
}

  if (screen === 'history') {
    return (
      <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
        <Text style={styles.logo}>Se’kret Bip</Text>
        <Text style={styles.welcome}>Mood History 📈</Text>
        <Text style={styles.subtitle}>Your feelings have a story. Bip remembers gently.</Text>

        {moodHistory.length === 0 ? (
          <View style={styles.messageCard}>
            <Text style={styles.cardEmoji}>📊</Text>
            <Text style={styles.cardText}>Choose a mood on Home to start your history.</Text>
          </View>
        ) : (
          moodHistory.map((item) => (
            <View key={item.id} style={styles.entryCard}>
              <Text style={styles.entryText}>{item.mood}</Text>
              <Text style={styles.entryDate}>{item.date} • {item.time}</Text>
            </View>
          ))
        )}

        <BottomNav />
      </ScrollView>
    );
  }

  if (screen === 'voice') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>Voice Notes 🎙️</Text>

      <Text style={styles.subtitle}>
        {isRecording
          ? 'Se’kret is listening... let your orb speak.'
          : 'Tap record when you’re ready to Bip it out.'}
      </Text>

      <Animated.View
        style={[
          styles.circle,
          {
            transform: [{ scale: breatheAnim }],
            marginBottom: 30,
            backgroundColor: isRecording ? '#EF4444' : '#3B82F6',
          },
        ]}
      >
        <Text style={styles.circleText}>
          {isRecording ? '🔴' : '🎙️'}
        </Text>
      </Animated.View>

      {isRecording && (
        <View style={styles.waveRow}>
          <Animated.View
            style={[
              styles.waveBar,
              {
                transform: [{ scaleY: breatheAnim }],
              },
            ]}
          />

          <Animated.View
            style={[
              styles.waveBar,
              {
                height: 60,
                transform: [{
                  scaleY: breatheAnim.interpolate({
                    inputRange: [1, 1.1],
                    outputRange: [1, 1.6],
                  }),
                }],
              },
            ]}
          />

          <Animated.View
            style={[
              styles.waveBar,
              {
                height: 35,
                transform: [{
                  scaleY: breatheAnim.interpolate({
                    inputRange: [1, 1.1],
                    outputRange: [1, 1.3],
                  }),
                }],
              },
            ]}
          />

          <Animated.View
            style={[
              styles.waveBar,
              {
                height: 75,
                transform: [{
                  scaleY: breatheAnim.interpolate({
                    inputRange: [1, 1.1],
                    outputRange: [1, 1.8],
                  }),
                }],
              },
            ]}
          />

          <Animated.View
            style={[
              styles.waveBar,
              {
                height: 45,
                transform: [{
                  scaleY: breatheAnim.interpolate({
                    inputRange: [1, 1.1],
                    outputRange: [1, 1.4],
                  }),
                }],
              },
            ]}
          />
        </View>
      )}

      <TouchableOpacity
        style={[styles.button, isRecording && { backgroundColor: '#EF4444' }]}
        onPress={() => {
          if (isRecording) {
            saveVoiceNote();
          }

          setIsRecording(!isRecording);
        }}
      >
        <Text style={styles.buttonText}>
          {isRecording ? 'Stop Recording' : 'Start Bippin 🎙️'}
        </Text>
      </TouchableOpacity>

      <View style={styles.messageCard}>
        <Text style={styles.cardEmoji}>🌙</Text>
        <Text style={styles.cardText}>
          {isRecording
            ? 'No pressure. No perfect words. Just let it out.'
            : 'Your voice notes are little pieces of your story.'}
        </Text>
      </View>

      <Text style={styles.sectionTitle}>Saved Bips</Text>

      {voiceNotes.length === 0 ? (
        <View style={styles.entryCard}>
          <Text style={styles.entryText}>
            No voice Bips yet. When you stop recording, Se’kret will save the moment here.
          </Text>
        </View>
      ) : (
        voiceNotes.map((note) => (
          <View key={note.id} style={styles.entryCard}>
            <Text style={styles.entryText}>🎧 {note.title}</Text>
            <Text style={styles.entryDate}>{note.date} • {note.time}</Text>
          </View>
        ))
      )}

      <BottomNav />
    </ScrollView>
  );
}

if (screen === 'circle') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>

      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>Bip Circle 🌐</Text>

      <Text style={styles.subtitle}>
        A soft anonymous space to vent, heal, and connect.
      </Text>

      <TextInput
        style={styles.journalInput}
        placeholder="Share something with the Circle..."
        placeholderTextColor="#94A3B8"
        multiline
        value={circlePostText}
        onChangeText={setCirclePostText}
      />

      <TouchableOpacity
        style={styles.button}
        onPress={saveCirclePost}
      >
        <Text style={styles.buttonText}>Post to Circle ✨</Text>
      </TouchableOpacity>

      <TouchableOpacity
  style={styles.button}
  onPress={() => setScreen('bity')}
>
  <Text style={styles.buttonText}>🏙️ My Bity</Text>
</TouchableOpacity>
      
  <Text style={styles.sectionTitle}>Recent Bips</Text>

      {circlePosts.length === 0 ? (
        <View style={styles.entryCard}>
          <Text style={styles.entryText}>
            No Circle posts yet. Your words could help someone tonight.
          </Text>
        </View>
      ) : (
        circlePosts.map((post) => (
          <View key={post.id} style={styles.entryCard}>

            <Text style={styles.entryDate}>
              {post.date} • {post.time}
            </Text>

            <Text style={styles.entryText}>
              {post.text}
            </Text>

            <View style={styles.reactionRow}>

            <TouchableOpacity
  style={styles.reactionButton}
  onPress={() => reactToPost(post.id, 'glow')}
>
                <Text style={styles.reactionText}>
                  ✨ {post.reactions.glow}
                </Text>
              </TouchableOpacity>

              <TouchableOpacity
  style={styles.reactionButton}
  onPress={() => reactToPost(post.id, 'heart')}
> <Text style={styles.reactionText}>
                  💙 {post.reactions.heart}
                </Text>
              </TouchableOpacity>

            <TouchableOpacity
  style={styles.reactionButton}
  onPress={() => reactToPost(post.id, 'moon')}
>
                <Text style={styles.reactionText}>
                  🌙 {post.reactions.moon}
                </Text>
              </TouchableOpacity>

            </View>

          </View>
        ))
      )}

      <BottomNav />
    </ScrollView>
  );
}

if (screen === 'bity') {
  return (
    <ScrollView
      contentContainerStyle={[
        styles.container,
        { backgroundColor: getBackground() },
      ]}
    >

      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>🏙️ My Bity</Text>

      <Text style={styles.subtitle}>
        Your people. Your peace. Your support system.
      </Text>

      <View style={styles.bityHeroCard}>

        <Text style={styles.bityHeroEmoji}>💜</Text>

        <Text style={styles.bityHeroTitle}>
          Built on trust, not control.
        </Text>

        <Text style={styles.bityHeroText}>
          Your Bity is your emotional neighborhood —
          trusted peers, shared growth, and support that actually feels safe.
        </Text>

      </View>

      <Text style={styles.sectionTitle}>
        ✨ Bity Energy
      </Text>

      <View style={styles.statsRow}>

        <View style={styles.statCard}>
          <Text style={styles.statEmoji}>🌱</Text>
          <Text style={styles.statNumber}>4</Text>
          <Text style={styles.statLabel}>Shared Growth</Text>
        </View>

        <View style={styles.statCard}>
          <Text style={styles.statEmoji}>👥</Text>
          <Text style={styles.statNumber}>8</Text>
          <Text style={styles.statLabel}>Trusted Peers</Text>
        </View>

      </View>

      <View style={styles.statsRow}>

        <View style={styles.statCard}>
          <Text style={styles.statEmoji}>💬</Text>
          <Text style={styles.statNumber}>12</Text>
          <Text style={styles.statLabel}>Real Talks</Text>
        </View>

        <View style={styles.statCard}>
          <Text style={styles.statEmoji}>✨</Text>
          <Text style={styles.statNumber}>5</Text>
          <Text style={styles.statLabel}>Support Streak</Text>
        </View>

      </View>

      <Text style={styles.sectionTitle}>
        🫂 Peer Requests
      </Text>

      <View style={styles.entryCard}>

        <Text style={styles.entryText}>
          💜 Ava wants to join your Bity.
        </Text>

        <View style={styles.requestButtonRow}>

          <TouchableOpacity style={styles.acceptButton}>
            <Text style={styles.requestButtonText}>
              Accept
            </Text>
          </TouchableOpacity>

          <TouchableOpacity style={styles.declineButton}>
            <Text style={styles.requestButtonText}>
              Decline
            </Text>
          </TouchableOpacity>

        </View>

      </View>

      <Text style={styles.sectionTitle}>
        👥 Trusted People
      </Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>
          💜 Ava
        </Text>

        <Text style={styles.entryDate}>
          “Always checking in.”
        </Text>
      </View>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>
          🌙 Marcus
        </Text>

        <Text style={styles.entryDate}>
          “Late night calm buddy.”
        </Text>
      </View>

      <Text style={styles.sectionTitle}>
        💜 Parent Bridge
      </Text>

      <View style={styles.parentBridgeCard}>

        <Text style={styles.cardEmoji}>
          💜
        </Text>

        <Text style={styles.cardText}>
          Parent Bridge Active
        </Text>

        <Text style={styles.bridgeText}>
          Shared growth this week: 78%
        </Text>

        <Text style={styles.bridgeText}>
          Communication streak: 5 days
        </Text>
  
 <TouchableOpacity
  style={styles.button}
  onPress={() => setScreen('parentBridge')}
>
  <Text style={styles.buttonText}>
    Se’krets 2 Tell 💜
  </Text>
</TouchableOpacity>

      </View>

      <Text style={styles.sectionTitle}>
        🌱 Shared Growth
      </Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>
          ✨ 5 supportive check-ins completed
        </Text>

        <Text style={styles.entryText}>
          🌱 3 shared goals completed
        </Text>

        <Text style={styles.entryText}>
          💬 2 meaningful conversations this week
        </Text>
      </View>

      <BottomNav />

    </ScrollView>
  );
}
 if (screen === 'parentPortal') {
  return (
    <ScrollView
      contentContainerStyle={[
        styles.container,
        { backgroundColor: getBackground() },
      ]}
    >

      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>👨‍👩‍👧 Parent Portal</Text>

      <Text style={styles.subtitle}>
        Support without pressure. Connection without control.
      </Text>

      <View style={styles.parentBridgeCard}>

        <Text style={styles.cardEmoji}>💜</Text>

        <Text style={styles.cardText}>
          Your role is support, not surveillance.
        </Text>

        <Text style={styles.bridgeText}>
          Se’kret Bip protects private emotional spaces while helping families communicate better.
        </Text>

      </View>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('parentBridge')}
      >
        <Text style={styles.buttonText}>
          💜 Se’krets 2 Tell
        </Text>
      </TouchableOpacity>

      <TouchableOpacity
  style={styles.button}
  onPress={() => setScreen('parentReflections')}
>
  <Text style={styles.buttonText}>
    💭 Parent Reflections
  </Text>
</TouchableOpacity>

 <TouchableOpacity
  style={styles.button}
  onPress={() => setScreen('conversationKits')}
>
  <Text style={styles.buttonText}>
    🧠 Conversation Kits
  </Text>
</TouchableOpacity>

     <TouchableOpacity
  style={styles.button}
  onPress={() => setScreen('supportAlerts')}
>
  <Text style={styles.buttonText}>
    🚨 Support Alerts
  </Text>
</TouchableOpacity>

      <TouchableOpacity
  style={styles.button}
  onPress={() => setScreen('sharedGrowth')}
>
  <Text style={styles.buttonText}>
    🌱 Shared Growth
  </Text>
</TouchableOpacity>

      <BottomNav />

    </ScrollView>
  );
}

if (screen === 'parentBridge') {
  return (
    <ScrollView
      contentContainerStyle={[
        styles.container,
        { backgroundColor: getBackground() },
      ]}
    >
      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>💜 Se’krets 2 Tell</Text>

      <Text style={styles.subtitle}>
        Growth between us. Built on trust, not control.
      </Text>

      <View style={styles.parentBridgeCard}>
        <Text style={styles.cardEmoji}>💜</Text>

        <Text style={styles.cardText}>
          Parent Bridge Active
        </Text>

        <Text style={styles.bridgeText}>
          Parents get support signals, not your private Se’kret space.
        </Text>

        <Text style={styles.bridgeText}>
          Mood energy today: Soft / Low
        </Text>
      </View>

      <Text style={styles.sectionTitle}>🌱 Growth Between Us</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>🔥 Connection streak: 5 days</Text>
        <Text style={styles.entryText}>💬 Real talks this week: 2</Text>
        <Text style={styles.entryText}>🌙 Bip Calmly nights: 3</Text>
        <Text style={styles.entryText}>✨ Support score: 78%</Text>
      </View>

      <Text style={styles.sectionTitle}>📊 Parent Insights</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>Current vibe: Needs gentleness</Text>
        <Text style={styles.entryText}>Best support style: Encourage, don’t pressure</Text>
        <Text style={styles.entryText}>Helpful ask: “Want advice or just company?”</Text>
      </View>

      <Text style={styles.sectionTitle}>💬 Conversation Tips</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>Instead of: “What’s wrong?”</Text>
        <Text style={styles.entryText}>Try: “I’m here. Do you want space or support?”</Text>
      </View>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>Instead of: “You never talk to me.”</Text>
        <Text style={styles.entryText}>Try: “I miss hearing your thoughts.”</Text>
      </View>

      <Text style={styles.sectionTitle}>🎯 Shared Goals</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>✅ Have one calm check-in this week</Text>
        <Text style={styles.entryText}>✅ Do one tech-free dinner</Text>
        <Text style={styles.entryText}>⬜ Take a walk together</Text>
      </View>

      <Text style={styles.sectionTitle}>🔐 Mutual Share Promise</Text>

<View style={styles.entryCard}>
  <Text style={styles.entryText}>
    💜 Both sides keep private thoughts private.
  </Text>

  <Text style={styles.entryText}>
    ✍️ Se’kret Pages stay yours.
  </Text>

  <Text style={styles.entryText}>
    🎙️ Bippin BRB stays yours.
  </Text>

  <Text style={styles.entryText}>
    🌱 Only shared moments cross the bridge.
  </Text>

  <Text style={styles.entryText}>
    💜 Built to understand you, not monitor you.
  </Text>
</View>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('bity')}
      >
        <Text style={styles.buttonText}>Back to My Bity 🏙️</Text>
      </TouchableOpacity>

      <BottomNav />
    </ScrollView>
  );
}
 
 if (screen === 'parentReflections') {
  return (
    <ScrollView
      contentContainerStyle={[
        styles.container,
        { backgroundColor: getBackground() },
      ]}
    >

      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>💭 Parent Reflections</Text>

      <Text style={styles.subtitle}>
        A private space for parents to pause, reflect, and respond with care.
      </Text>

      <View style={styles.messageCard}>

        <Text style={styles.cardEmoji}>💜</Text>

        <Text style={styles.cardText}>
          Parenting while emotionally exhausted is still parenting.
        </Text>

      </View>

      <View style={styles.entryCard}>

        <Text style={styles.entryText}>
          ✍️ Reflect before reacting.
        </Text>

        <Text style={styles.entryText}>
          🌙 Your child may need safety before solutions.
        </Text>

        <Text style={styles.entryText}>
          🧠 Curiosity connects better than pressure.
        </Text>

      </View>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('parentPortal')}
      >
        <Text style={styles.buttonText}>
          Back to Parent Portal 👨‍👩‍👧
        </Text>
      </TouchableOpacity>

      <BottomNav />

    </ScrollView>
  );
}
 
 if (screen === 'conversationKits') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>🧠 Conversation Kits</Text>

      <Text style={styles.subtitle}>
        Softer words for hard moments.
      </Text>

      <View style={styles.messageCard}>
        <Text style={styles.cardEmoji}>💜</Text>

        <Text style={styles.cardText}>
          The goal is connection, not winning.
        </Text>
      </View>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>
          Instead of: “What’s wrong with you?”
        </Text>

        <Text style={styles.entryText}>
          Try: “I can tell something feels heavy. Want space or support?”
        </Text>
      </View>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>
          Instead of: “You never talk to me.”
        </Text>

        <Text style={styles.entryText}>
          Try: “I miss hearing your thoughts. No pressure, I’m here.”
        </Text>
      </View>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('parentPortal')}
      >
        <Text style={styles.buttonText}>
          Back to Parent Portal 👨‍👩‍👧
        </Text>
      </TouchableOpacity>

      <BottomNav />
    </ScrollView>
  );
}

if (screen === 'sharedGrowth') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>

      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>🌱 Shared Growth</Text>

      <Text style={styles.subtitle}>
        Progress together, not perfection.
      </Text>

      <View style={styles.parentBridgeCard}>
        <Text style={styles.cardEmoji}>🌱</Text>

        <Text style={styles.cardText}>
          Healing relationships count too.
        </Text>

        <Text style={styles.bridgeText}>
          Se’kret tracks supportive moments — not punishments or mistakes.
        </Text>
      </View>

      <Text style={styles.sectionTitle}>
        ✨ This Week
      </Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>
          💬 3 calm conversations completed
        </Text>

        <Text style={styles.entryText}>
          🌙 2 Bip Calmly resets shared
        </Text>

        <Text style={styles.entryText}>
          💜 5 supportive responses sent
        </Text>

        <Text style={styles.entryText}>
          🌱 1 hard moment handled safely
        </Text>
      </View>

      <Text style={styles.sectionTitle}>
        🫂 Emotional Wins
      </Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>
          ✨ Teen opened up first
        </Text>

        <Text style={styles.entryText}>
          💜 Parent paused before reacting
        </Text>

        <Text style={styles.entryText}>
          🌙 Conflict ended calmly
        </Text>
      </View>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('parentPortal')}
      >
        <Text style={styles.buttonText}>
          Back to Parent Portal 👨‍👩‍👧
        </Text>
      </TouchableOpacity>

      <BottomNav />

    </ScrollView>
  );
}
 
 if (screen === 'supportAlerts') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>🚨 Support Alerts</Text>

      <Text style={styles.subtitle}>
        Care signals, not punishment.
      </Text>

      <View style={styles.parentBridgeCard}>
        <Text style={styles.cardEmoji}>🌙</Text>

        <Text style={styles.cardText}>
          No emergency alerts right now.
        </Text>

        <Text style={styles.bridgeText}>
          If Se’kret notices a moment that needs grown-up support, it appears here with gentle next steps.
        </Text>
      </View>

      <Text style={styles.sectionTitle}>Example Care Signal</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>
          💜 Teen may need a softer check-in tonight.
        </Text>

        <Text style={styles.entryText}>
          Try: “I’m not mad. I just want to understand what support feels best right now.”
        </Text>
      </View>

      <Text style={styles.sectionTitle}>Emergency Conversation Kit</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>1️⃣ Start calm.</Text>
        <Text style={styles.entryText}>2️⃣ Ask before advising.</Text>
        <Text style={styles.entryText}>3️⃣ Offer presence, not pressure.</Text>
      </View>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('parentPortal')}
      >
        <Text style={styles.buttonText}>
          Back to Parent Portal 👨‍👩‍👧
        </Text>
      </TouchableOpacity>

      <BottomNav />
    </ScrollView>
  );
}  if (screen === 'comfort') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>🚨 Extra Support</Text>

      <Text style={styles.subtitle}>
        When it feels too loud, Se’kret stays with you.
      </Text>

      <Animated.View
        style={[
          styles.circle,
          { transform: [{ scale: breatheAnim }], backgroundColor: '#7C3AED' },
        ]}
      >
        <Text style={styles.circleText}>Stay</Text>
      </Animated.View>

      <View style={styles.messageCard}>
        <Text style={styles.cardEmoji}>💜</Text>
        <Text style={styles.cardText}>
          You do not have to handle this moment perfectly.
        </Text>
      </View>

      <Text style={styles.sectionTitle}>🫧 Ground Right Now</Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>1️⃣ Put both feet on the floor.</Text>
        <Text style={styles.entryText}>2️⃣ Name 3 things you can see.</Text>
        <Text style={styles.entryText}>3️⃣ Touch something soft or solid.</Text>
        <Text style={styles.entryText}>4️⃣ Take one slow breath.</Text>
        <Text style={styles.entryText}>5️⃣ Say: “This moment will pass.”</Text>
      </View>

      <Text style={[styles.sectionTitle, { alignSelf: 'center', textAlign: 'center' }]}>
        💬 What do you need?
      </Text>

      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>🌙 I need calm</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>💜 I need comfort</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>🫂 I need someone</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.button} onPress={() => setScreen('musicVibes')}>
        <Text style={styles.buttonText}>🎵 I need music</Text>
      </TouchableOpacity>

      <View style={styles.parentBridgeCard}>
        <Text style={styles.cardEmoji}>🎵</Text>
        <Text style={styles.cardText}>Page Vibes</Text>
        <Text style={styles.bridgeText}>
          Future idea: Se’kret can let songs play softly in the background of certain pages.
        </Text>
        <Text style={styles.bridgeText}>
          MySpace energy, but calmer and safer.
        </Text>
      </View>

      <TouchableOpacity style={styles.button} onPress={() => setScreen('calm')}>
        <Text style={styles.buttonText}>Back to Bip Calmly 🌙</Text>
      </TouchableOpacity>

      <BottomNav />
    </ScrollView>
  );
}if (screen === 'musicVibes') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>🎵 Music Vibes</Text>

      <Text style={styles.subtitle}>
        Pick the sound your page needs.
      </Text>

      <View style={styles.parentBridgeCard}>
        <Text style={styles.cardEmoji}>🎧</Text>
        <Text style={styles.cardText}>Se’kret Soundtrack</Text>
        <Text style={styles.bridgeText}>
          Later, this can connect to Spotify or Apple Music and suggest songs based on your mood.
        </Text>
      </View>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>🏠 Home: soft welcome track</Text>
        <Text style={styles.entryText}>💜 Pages: diary writing track</Text>
        <Text style={styles.entryText}>🌙 Bip Calmly: calm ambient track</Text>
        <Text style={styles.entryText}>🚨 Extra Support: grounding track</Text>
        <Text style={styles.entryText}>🌐 Bip Circle: late-night community track</Text>
      </View>

      <TouchableOpacity style={styles.button} onPress={() => setScreen('comfort')}>
        <Text style={styles.buttonText}>Back to Extra Support 🚨</Text>
      </TouchableOpacity>

      <BottomNav />
    </ScrollView>
  );
}

  if (screen === 'more') {
  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>

      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>More ✨</Text>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('growth')}
      >
        <Text style={styles.buttonText}>🌱 Growth Garden</Text>
      </TouchableOpacity>
      
    <TouchableOpacity
  style={[
    styles.button,
    userRole === 'teen' && {
      backgroundColor: '#475569',
      opacity: 0.6,
    },
  ]}
  onPress={() => {
    if (userRole === 'parent') {
      setScreen('parentPortal');
    }
  }}
>
  <Text style={styles.buttonText}>
    {userRole === 'teen'
      ? '🔒 Parent Portal'
      : '👨‍👩‍👧 Parent Portal'}
  </Text>
</TouchableOpacity>

<TouchableOpacity style={styles.button} onPress={switchRole}>
  <Text style={styles.buttonText}>
    {userRole === 'teen'
      ? '🔄 Switch to Parent Mode'
      : '🔄 Switch to Teen Mode'}
  </Text>
</TouchableOpacity>

<TouchableOpacity
  style={[
    styles.button,
    userRole === 'teen' && {
      backgroundColor: '#475569',
      opacity: 0.6,
    },
  ]}
  onPress={() => {
    if (userRole === 'parent') {
      setScreen('parentPortal');
    }
  }}
>
  <Text style={styles.buttonText}>
    {userRole === 'teen'
      ? '🔒 Parent Portal'
      : '👨‍👩‍👧 Parent Portal'}
  </Text>
</TouchableOpacity>

      <TouchableOpacity
        style={styles.button}
        onPress={() => setScreen('settings')}
      >
        <Text style={styles.buttonText}>⚙️ Settings</Text>
      </TouchableOpacity>

      <BottomNav />
    </ScrollView>
  );
}

  if (screen === 'settings') {
  return (
    <ScrollView
      contentContainerStyle={[
        styles.container,
        { backgroundColor: getBackground() },
      ]}
    >

      <Text style={styles.logo}>Se’kret Bip</Text>

      <Text style={styles.welcome}>⚙️ Settings</Text>

      <Text style={styles.subtitle}>
        Customize your Bip world.
      </Text>

      <Text style={styles.sectionTitle}>
        🌈 Theme Vibes
      </Text>

      <View style={styles.statsRow}>

        <TouchableOpacity
          style={styles.statCard}
          onPress={() => setTheme('flower')}
        >
          <Text style={styles.statEmoji}>🌸</Text>

          <Text style={styles.statLabel}>
            Neon Pink
          </Text>
        </TouchableOpacity>

        <TouchableOpacity
          style={styles.statCard}
          onPress={() => setTheme('rain')}
        >
          <Text style={styles.statEmoji}>🌧️</Text>

          <Text style={styles.statLabel}>
            Neon Blue
          </Text>
        </TouchableOpacity>

      </View>

      <View style={styles.statsRow}>

        <TouchableOpacity
          style={styles.statCard}
          onPress={() => setTheme('forest')}
        >
          <Text style={styles.statEmoji}>🍀</Text>

          <Text style={styles.statLabel}>
            Neon Green
          </Text>
        </TouchableOpacity>

        <TouchableOpacity
          style={styles.statCard}
          onPress={() => setTheme('lavender')}
        >
          <Text style={styles.statEmoji}>🪻</Text>

          <Text style={styles.statLabel}>
            Lavender Glow
          </Text>
        </TouchableOpacity>

      </View>

      <Text style={styles.sectionTitle}>
        🌍 Language
      </Text>

      <View style={styles.entryCard}>
        <Text style={styles.entryText}>
          🇺🇸 English
        </Text>

        <Text style={styles.entryText}>
          🇪🇸 Español
        </Text>

        <Text style={styles.entryText}>
          🇫🇷 Français
        </Text>

        <Text style={styles.entryText}>
          🇵🇹 Português
        </Text>

        <Text style={styles.entryText}>
          🇭🇹 Kreyòl
        </Text>
      </View>

      <Text style={styles.sectionTitle}>
        🌙 Soft Shutdown
      </Text>

      <View style={styles.parentBridgeCard}>

        <Text style={styles.cardEmoji}>🌙</Text>

        <Text style={styles.cardText}>
          Night protection is ON
        </Text>

        <Text style={styles.bridgeText}>
          Bip Calmly stays open after 10PM.
        </Text>

        <Text style={styles.bridgeText}>
          Other spaces gently dim for rest.
        </Text>

      </View>

      <Text style={styles.sectionTitle}>
        🔐 Privacy Controls
      </Text>

      <View style={styles.entryCard}>

        <Text style={styles.entryText}>
          ✍️ Se’kret Pages locked
        </Text>

        <Text style={styles.entryText}>
          🎙️ Voice notes protected
        </Text>

        <Text style={styles.entryText}>
          💜 Shared Parent Bridge only
        </Text>

        <Text style={styles.entryText}>
          🌐 Bip Circle anonymous mode
        </Text>

      </View>

      <Text style={styles.sectionTitle}>
        ✨ Coming Soon
      </Text>

      <View style={styles.entryCard}>

        <Text style={styles.entryText}>
          🎨 Moodboard stickers
        </Text>

        <Text style={styles.entryText}>
          📸 Memory walls
        </Text>

        <Text style={styles.entryText}>
          🎵 Music reactions
        </Text>

        <Text style={styles.entryText}>
          🌌 Custom room aesthetics
        </Text>

      </View>

      <BottomNav />

    </ScrollView>
  );
}

  return (
    <ScrollView contentContainerStyle={[styles.container, { backgroundColor: getBackground() }]}>
      <Text style={styles.logo}>Se’kret Bip</Text>
      <Text style={styles.heroText}>{getHeroText()}</Text>

      <Text style={styles.subtitle}>
        A safe place for your emotions, thoughts, and growth.
      </Text>

      <View style={styles.messageCard}>
        <Text style={styles.cardEmoji}>{comfortMessages[messageIndex].emoji}</Text>
        <Text style={styles.cardText}>{comfortMessages[messageIndex].text}</Text>

        <TouchableOpacity style={styles.smallButton} onPress={changeMessage}>
          <Text style={styles.smallButtonText}>New Message ✨</Text>
        </TouchableOpacity>
      </View>

      <View style={styles.moodRow}>
        {[
          ['Happy', '😊'],
          ['Sad', '😔'],
          ['Angry', '😡'],
          ['Tired', '😴'],
        ].map((item) => (
          <TouchableOpacity
            key={item[0]}
            style={[styles.moodBubble, mood === item[0] && styles.selectedMoodBubble]}
            onPress={() => selectMood(item[0])}
          >
            <Text style={styles.moodEmoji}>{item[1]}</Text>
          </TouchableOpacity>
        ))}
      </View>

      <Text style={styles.currentMood}>Current Mood: {mood}</Text>

      <BottomNav />
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    alignItems: 'center',
    paddingHorizontal: 28,
    paddingTop: 80,
    paddingBottom: 60,
  },

  logo: {
  fontSize: 34,
  fontWeight: '900',
  color: '#FFFFFF',
  marginBottom: 40,
},

heroText: {
  fontSize: 42,
  fontWeight: '900',
  color: '#FFFFFF',
  textAlign: 'center',
  lineHeight: 52,
  marginBottom: 30,
},

welcome: {
  fontSize: 30,
  fontWeight: '900',
  color: '#FFFFFF',
  textAlign: 'center',
  marginBottom: 14,
},

subtitle: {
  fontSize: 18,
  color: '#CBD5E1',
  textAlign: 'center',
  lineHeight: 28,
  marginBottom: 40,
  fontWeight: '700',
},

cardEmoji: {
  fontSize: 42,
  marginBottom: 18,
},

cardText: {
  fontSize: 24,
  color: '#FFFFFF',
  fontWeight: '900',
  textAlign: 'center',
  lineHeight: 34,
  marginBottom: 24,
},
  messageCard: {
  width: '100%',
  backgroundColor: '#1E293B',
  padding: 24,
  borderRadius: 30,
  marginBottom: 28,
  alignItems: 'center',
},

button: {
  backgroundColor: '#3B82F6',
  width: '100%',
  paddingVertical: 18,
  borderRadius: 22,
  alignItems: 'center',
  marginBottom: 22,
},

smallButton: {
  backgroundColor: '#334155',
  paddingVertical: 10,
  paddingHorizontal: 18,
  borderRadius: 18,
},

smallButtonText: {
  color: '#FFFFFF',
  fontWeight: '800',
  fontSize: 15,
},

journalInput: {
  width: '100%',
  minHeight: 180,
  backgroundColor: '#1E293B',
  borderRadius: 24,
  padding: 20,
  color: '#FFFFFF',
  fontSize: 17,
  textAlignVertical: 'top',
  marginBottom: 24,
},

entryCard: {
  width: '100%',
  backgroundColor: '#1E293B',
  borderRadius: 24,
  padding: 20,
  marginBottom: 18,
},

moodRow: {
  flexDirection: 'row',
  gap: 14,
  marginBottom: 24,
},

moodBubble: {
  width: 70,
  height: 70,
  borderRadius: 35,
  backgroundColor: '#1E293B',
  alignItems: 'center',
  justifyContent: 'center',
},

selectedMoodBubble: {
  backgroundColor: '#3B82F6',
},

moodEmoji: {
  fontSize: 30,
},

themeRow: {
  flexDirection: 'row',
  gap: 18,
  marginBottom: 30,
},

themeBubble: {
  width: 80,
  height: 80,
  borderRadius: 40,
  backgroundColor: '#1E293B',
  alignItems: 'center',
  justifyContent: 'center',
},

themeEmoji: {
  fontSize: 34,
},

buttonText: {
  color: '#FFFFFF',
  fontSize: 22,
  fontWeight: '900',
},

currentMood: {
  fontSize: 22,
  fontWeight: '900',
  color: '#FFFFFF',
  marginBottom: 32,
  textAlign: 'center',
},

sectionTitle: {
  fontSize: 26,
  fontWeight: '900',
  color: '#FFFFFF',
  alignSelf: 'flex-start',
  marginBottom: 16,
},

entryText: {
  color: '#FFFFFF',
  fontSize: 17,
  lineHeight: 26,
  fontWeight: '700',
  marginBottom: 8,
},

circleText: {
  color: '#FFFFFF',
  fontSize: 22,
  fontWeight: '900',
},

entryDate: {
  color: '#CBD5E1',
  fontSize: 14,
  fontWeight: '800',
  marginBottom: 8,
},

circle: {
  width: 170,
  height: 170,
  borderRadius: 85,
  backgroundColor: '#3B82F6',
  alignItems: 'center',
  justifyContent: 'center',
  marginBottom: 55,
  shadowColor: '#60A5FA',
  shadowOffset: {
    width: 0,
    height: 0,
  },
  shadowOpacity: 0.9,
  shadowRadius: 25,
  elevation: 18,
},
waveRow: {
  flexDirection: 'row',
  alignItems: 'flex-end',
  justifyContent: 'center',
  gap: 8,
  marginBottom: 35,
},

waveBar: {
  width: 10,
  height: 50,
  backgroundColor: '#60A5FA',
  borderRadius: 20,
},
  
  reactionRow: {
  flexDirection: 'row',
  marginTop: 18,
  gap: 12,
},

reactionButton: {
  backgroundColor: '#334155',
  paddingVertical: 8,
  paddingHorizontal: 14,
  borderRadius: 20,
},

reactionText: {
  color: '#FFFFFF',
  fontWeight: '800',
  fontSize: 14,
},

    bottomNav: {
    width: '100%',
    backgroundColor: '#1E293B',
    borderRadius: 28,
    paddingVertical: 14,
    paddingHorizontal: 6,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: 20,
  },

 navItem: {
  alignItems: 'center',
  width: '20%',
},

  navIcon: {
    fontSize: 22,
    textAlign: 'center',
  },

  navText: {
    color: '#E2E8F0',
    fontSize: 14,
    fontWeight: '700',
    textAlign: 'center',
    marginTop: 4,
  },

  activeNavText: {

    color: '#FFFFFF',

  },

  bityHeroCard: {
  width: '100%',
  backgroundColor: '#4C1D95',
  padding: 28,
  borderRadius: 34,
  marginBottom: 28,
  alignItems: 'center',
},

bityHeroEmoji: {
  fontSize: 46,
  marginBottom: 16,
},

bityHeroTitle: {
  color: '#FFFFFF',
  fontSize: 24,
  fontWeight: '900',
  textAlign: 'center',
  marginBottom: 14,
},

bityHeroText: {
  color: '#E9D5FF',
  fontSize: 17,
  fontWeight: '700',
  textAlign: 'center',
  lineHeight: 28,
},

statsRow: {
  width: '100%',
  flexDirection: 'row',
  justifyContent: 'space-between',
  marginBottom: 18,
},

statCard: {
  width: '48%',
  backgroundColor: '#1E293B',
  borderRadius: 28,
  paddingVertical: 24,
  paddingHorizontal: 14,
  alignItems: 'center',
},

statEmoji: {
  fontSize: 30,
  marginBottom: 10,
},

statNumber: {
  color: '#FFFFFF',
  fontSize: 28,
  fontWeight: '900',
  marginBottom: 8,
},

statLabel: {
  color: '#CBD5E1',
  fontSize: 14,
  fontWeight: '700',
  textAlign: 'center',
},

requestButtonRow: {
  flexDirection: 'row',
  gap: 14,
  marginTop: 18,
},

acceptButton: {
  flex: 1,
  backgroundColor: '#3B82F6',
  paddingVertical: 14,
  borderRadius: 18,
  alignItems: 'center',
},

declineButton: {
  flex: 1,
  backgroundColor: '#334155',
  paddingVertical: 14,
  borderRadius: 18,
  alignItems: 'center',
},

requestButtonText: {
  color: '#FFFFFF',
  fontWeight: '900',
  fontSize: 16,
},

parentBridgeCard: {
  width: '100%',
  backgroundColor: '#312E81',
  borderRadius: 34,
  padding: 28,
  marginBottom: 28,
  alignItems: 'center',
},

bridgeText: {
  color: '#E0E7FF',
  fontSize: 16,
  fontWeight: '700',
  marginBottom: 12,
  textAlign: 'center',
},

});